---
version: "2.0"

services:
  bitbucket:
    image: atlassian/bitbucket-server:latest
    expose:
      - port: 7990
        as: 80
        to:
          - global: true

profiles:
  compute:
    bitbucket:
      resources:
        cpu:
          units: 0.8
        memory:
          size: 1Gi
        storage:
          size: 2Gi
  placement:
    akash:
      pricing:
        bitbucket: 
          denom: uakt
          amount: 100

deployment:
  bitbucket:
    akash:
      profile: bitbucket
      count: 1
