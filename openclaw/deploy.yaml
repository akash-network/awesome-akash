---
version: "2.0"
services:
  openclaw:
    image: ghcr.io/zjuuu/openclaw-docker:openclaw-v2026.2.9@sha256:3812dc10248395a11720b9111b1ac98215b641fa96d4e138c821763d3dbd32ca
    env:
      # REQUIRED: Set a strong password for the setup UI
      - SETUP_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
      - OPENCLAW_CONFIG_PATH=/data/.openclaw/openclaw.json
    expose:
      - port: 8080
        as: 80
        to:
          - global: true
        http_options:
          max_body_size: 104857600
          read_timeout: 60000
          send_timeout: 60000
          next_cases:
            - error
            - timeout
          next_tries: 3
          next_timeout: 60000
    params:
      storage:
        data:
          mount: /data
          readOnly: false
profiles:
  compute:
    openclaw:
      resources:
        cpu:
          units: 3
        memory:
          size: 6Gi
        storage:
          # Ephemeral storage for container
          - size: 5Gi
          # Persistent storage for data
          - name: data
            size: 10Gi
            attributes:
              persistent: true
              class: beta3
  placement:
    akash:
      pricing:
        openclaw:
          denom: uakt
          amount: 1000
deployment:
  openclaw:
    akash:
      profile: openclaw
      count: 1