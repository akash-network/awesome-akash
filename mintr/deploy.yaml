---
version: "2.0"

services:
  mintr:
    image: yykpk0923/mintr:v2
    expose:
      - port: 80
        as: 80
        to:
          - global: true

profiles:
  compute:
    mintr:
      resources:
        cpu:
          units: 1.0
        memory:
          size: 512Mi
        storage:
          size: 512Mi
  placement:
    akash:
      pricing:
        mintr: 
          denom: uakt
          amount: 100

deployment:
  mintr:
    akash:
      profile: mintr
      count: 1
