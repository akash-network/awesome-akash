---
version: "2.0"

services:
  augur:
    image: augurproject/augur
    expose:
      - port: 80
        as: 80
        to:
          - global: true

profiles:
  compute:
    augur:
      resources:
        cpu:
          units: 0.1
        memory:
          size: 512Mi
        storage:
          size: 512Mi
  placement:
    akash:
      pricing:
        augur: 
          denom: uakt
          amount: 100

deployment:
  augur:
    akash:
      profile: augur
      count: 1
