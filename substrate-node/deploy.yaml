---
version: "2.0"

services:
  web:
    image: ubinix5warun/substrate-node:v3-dev
    expose:
      - port: 9944
        as: 9944
        to:
          - global: true

profiles:
  compute:
    web:
      resources:
        cpu:
          units: 1.0
        memory:
          size: 512Mi
        storage:
          size: 512Mi
  placement:
    akash:
      pricing:
        substrate: 
          denom: uakt
          amount: 100

deployment:
  web:
    akash:
      profile: web
      count: 1
